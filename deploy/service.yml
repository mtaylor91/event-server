apiVersion: v1
kind: Service
metadata:
  name: event-service
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: http
  - name: http-monitoring
    port: 8081
    protocol: TCP
    targetPort: http-monitoring
